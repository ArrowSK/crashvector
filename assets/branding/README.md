# CrashVector branding

`crashvector-icon.webp` is the canonical application and repository icon source. It must remain a valid, high-resolution square image; platform-specific icon containers are generated during packaging from this source rather than committed as hand-edited derivatives.
